@model List<LimpRegistroDetalle>

<form method="post" asp-action="Envio" class="form-horizontal">
    <input type="hidden" name="CodArea" value='@TempData["Area"]' />
    <input type="hidden" name="CodFormulario" value='@TempData["Formulario"]' />
    <input type="hidden" name="NumFormulario" value='@TempData["NumFormulario"]' />
    <input type="hidden" name="CodEmpresa" value='@TempData["Empresa"]' />
    @foreach (var item in (TempData["User"] as List<string>))
    {
        <input type="hidden" name="User" value="@item" />
    }

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="card-header">
                        <h5>Usuario: @((TempData["User"] as List<string>)?.FirstOrDefault())</h5>
                        <h5>Formulario: @TempData["Area"]</h5>
                        <h5>Formulario: @TempData["NumFormulario"]</h5>
                        <h3 class="card-title">Tabla de formularios</h3>
                        <div class="card-tools">
                            <div class="input-group input-group-sm" style="width: 150px;">
                                <input type="text" name="table_search" class="form-control float-right" placeholder="Search">
                                <div class="input-group-append">
                                    <button type="submit" class="btn btn-default">
                                        <i class="fas fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-body table-responsive p-0">
                    <table class="table table-hover text-nowrap">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Nombre</th>
                                <th>Check</th>
                            </tr>
                        </thead>
                        <tbody>
                            
                            @for (var i = 0; i < Model.Count; i++)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(m =>Model[i].CodActividad)</td>
                                    <td>@Html.DisplayFor(m =>Model[i].CodActividad)</td>
                                    <td>
                                        <div class="form-check">
                                            <input type="hidden" name="model[@i].CodActividad" value='@Model[i].CodActividad' />
                                            <input type="checkbox" class="form-check-input" name="model[@i].Realizado" value="true" @(Model[i].Realizado ? "checked" : "") />
                                            <label class="form-check-label @((Model[i].Realizado) ? "text-success" : "text-danger")">
                                                @(Model[i].Realizado ? "Realizado" : "No Realizado")
                                            </label>
                                        </div>
                                    </td>
                                </tr>
                            }
                            
                        </tbody>
                    </table>
                </div>
            </div>

            @if ((TempData["User"] as List<string>)?.ElementAtOrDefault(2) == "Responsable")
            {

            @if (TempData["Estado"].ToString() == "0")
            { 
                <button type="submit" class="btn btn-primary">Enviar</button>
            }
            else
            {
                <a class="btn btn-primary disabled" tabindex="-1" role="button" aria-disabled="true">Enviar</a>
            }

            }
            
        </div>
    </div>
</form>
@if ((TempData["User"] as List<string>)?.ElementAtOrDefault(2) == "Responsable")
{
    <form method="post" asp-action="Salir" class="d-flex">
        <input type="hidden" name="CodArea" value='@TempData["Area"]' />
        <input type="hidden" name="CodFormulario" value='@TempData["Formulario"]' />
        <input type="hidden" name="NumFormulario" value='@TempData["NumFormulario"]' />
        <input type="hidden" name="CodEmpresa" value='@TempData["Empresa"]' />
        <input type="hidden" name="Estado" value='@TempData["Estado"]' />
        @foreach (var item in (TempData["User"] as List<string>))
        {
            <input type="hidden" name="User" value="@item" />
        }
        <button type="submit" class="btn btn-danger">Atras</button>
    </form>
}
else if ((TempData["User"] as List<string>)?.ElementAtOrDefault(2) == "super")
{
    <form method="post" asp-action="SalirSupervisor" class="d-flex">
        <input type="hidden" name="CodArea" value='@TempData["Area"]' />
        <input type="hidden" name="CodFormulario" value='@TempData["Formulario"]' />
        <input type="hidden" name="NumFormulario" value='@TempData["NumFormulario"]' />
        <input type="hidden" name="CodEmpresa" value='@TempData["Empresa"]' />
        <input type="hidden" name="Estado" value='@TempData["Estado"]' />
        @foreach (var item in (TempData["User"] as List<string>))
    {
        <input type="hidden" name="User" value="@item" />
    }
    <button type="submit" class="btn btn-danger">Atras</button>
</form>
}
@if ((TempData["User"] as List<string>
    )?.ElementAtOrDefault(2) == "super" )
{
    <form method="post" asp-action="EnviarVerificado" class="d-flex">
        <input type="hidden" name="CodArea" value='@TempData["Area"]' />
        <input type="hidden" name="CodFormulario" value='@TempData["Formulario"]' />
        <input type="hidden" name="NumFormulario" value='@TempData["NumFormulario"]' />
        <input type="hidden" name="CodEmpresa" value='@TempData["Empresa"]' />
        <input type="hidden" name="Estado" value='@TempData["Estado"]' />
        @foreach (var item in (TempData["User"] as List<string>))
        {
            <input type="hidden" name="User" value="@item" />
        }
        <button type="submit" class="btn btn-success">Verificado</button>
    </form>
}

